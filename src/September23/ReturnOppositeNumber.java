package September23;

public class ReturnOppositeNumber {
    public static int opposite(int number)
    {
        // if pos, return negative of it. conversely, if num is negative mult by -1 will turn it positive
        return number * -1;
    }
}
